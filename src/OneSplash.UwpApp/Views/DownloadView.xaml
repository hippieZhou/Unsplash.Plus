<UserControl
    x:Class="OneSplash.UwpApp.Views.DownloadView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:animations="using:Microsoft.Toolkit.Uwp.UI.Animations"
    xmlns:common="using:OneSplash.UwpApp.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:OneSplash.UwpApp.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid
        Margin="0,32,0,0"
        animations:Implicit.HideAnimations="{StaticResource DefaultHideAnimations}"
        animations:Implicit.ShowAnimations="{StaticResource DefaultShowAnimations}"
        Background="{ThemeResource SystemControlAcrylicElementBrush}"
        CornerRadius="40,40,0,0"
        Visibility="{x:Bind ViewModel.IsPaneShow, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
        <Button
            Width="48"
            Height="48"
            Margin="24,24,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Command="{x:Bind ViewModel.BackCommand}"
            Foreground="{ThemeResource SystemColorWindowTextBrush}"
            Style="{StaticResource Style-Button-Icon-Circle}">
            <ContentControl Width="20" Style="{StaticResource Style-Icon-ArrowLeft}" />
        </Button>
    </Grid>
</UserControl>
