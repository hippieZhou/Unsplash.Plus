<UserControl
    x:Class="OneSplash.UwpApp.Controls.GridViewHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:OneSplash.UwpApp.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">
    <ScrollViewer x:Name="Animated_ScrollViewer">
        <winui:ItemsRepeater x:Name="animatedScrollRepeater">
            <winui:ItemsRepeater.Layout>
                <winui:StackLayout Orientation="Horizontal" Spacing="12" />
            </winui:ItemsRepeater.Layout>
            <DataTemplate x:DataType="x:String">
                <Grid
                    Width="200"
                    Height="120"
                    Margin="12"
                    HorizontalAlignment="Stretch"
                    Background="{x:Bind}"
                    CornerRadius="6">
                    <TextBlock
                        VerticalAlignment="Bottom"
                        FontFamily="{StaticResource MontserratBold}"
                        Foreground="{StaticResource SystemBaseHighColor}"
                        Text="{x:Bind}" />
                </Grid>
            </DataTemplate>
        </winui:ItemsRepeater>
    </ScrollViewer>
</UserControl>
